spring.application.name=banco_api
server.port=8181

# DATA SOURCE
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:eventos_api}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=postgres

# JPA/HIBERNATE
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# RABBITMQ
spring.rabbitmq.host=${RABBIT_HOST:localhost}
spring.rabbitmq.username=${RABBIT_USERNAME:rabbitmq}
spring.rabbitmq.password=${RABBIT_PASSWORD:rabbitmq}
spring.rabbitmq.port=${RABBIT_PORT:5672}

# RABBITMQ Create Event
rabbitmq.exchange.create.name=events.v1.create-event
rabbitmq.exchange.create-notification.name=events.v1.create-notification-event
rabbitmq.exchange.error.create.name=events.v1.create-error-event
rabbitmq.create.queue.name=events.v1.create-event.save-database

# RABBITMQ Get Event
rabbitmq.exchange.get-all.name=events.v1.get-all-event
rabbitmq.get-all.queue.name=events.v1.get-all-event
