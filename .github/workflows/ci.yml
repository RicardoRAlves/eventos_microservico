# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Event Microservice

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master", "develop" ]

jobs:
  build-event-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Give permission for Maven
        run: chmod +x mvnw
        working-directory: ./event-api

      - name: Build tests
        run: ./mvnw clean verify --no-transfer-progress
        working-directory: ./event-api

  build-user-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Give permission for Maven
        run: chmod +x mvnw
        working-directory: ./user_api

      - name: Build tests
        run: ./mvnw clean verify --no-transfer-progress
        working-directory: ./user_api

  build-organization-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Give permission for Maven
        run: chmod +x mvnw
        working-directory: ./organization_api

      - name: Build tests
        run: ./mvnw clean verify --no-transfer-progress
        working-directory: ./organization_api

  build-processor-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Give permission for Maven
        run: chmod +x mvnw
        working-directory: ./processor_api

      - name: Build tests
        run: ./mvnw clean verify --no-transfer-progress
        working-directory: ./processor_api

  build-notification-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Give permission for Maven
        run: chmod +x mvnw
        working-directory: ./notification-api

      - name: Build tests
        run: ./mvnw clean verify --no-transfer-progress
        working-directory: ./notification-api
